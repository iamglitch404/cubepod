name: cubepod_benchmarks
description: A core component of the CubePod Application Runtime. Provides cubepod_benchmarks functionalities.
version: 0.1.1
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  cubepod_core: ^0.1.0
  cubepod_state: ^0.1.0
  cubepod_async: ^0.1.0
  cubepod_query: ^0.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.21.0
