name: cubepod_sync
description: Data synchronization tools for offline-first CubePod applications.
version: 0.1.5
repository: https://github.com/iamglitch404/cubepod

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  cubepod_storage: ^0.1.0
  cubepod_async: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
